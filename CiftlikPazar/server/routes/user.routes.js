const express = require('express');
const { protect } = require('../middleware/auth');

const router = express.Router();

// Burada profil işlemleri vardı, şimdilik kaldırıldı

module.exports = router; 