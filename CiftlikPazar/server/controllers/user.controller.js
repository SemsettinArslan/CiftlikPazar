const User = require('../models/user.model');

// İleride burada profil ve adres yönetimi için controller fonksiyonları olacak

// Örnek bir placeholder fonksiyon
exports.getUserExample = async (req, res) => {
  res.status(200).json({
    success: true,
    message: 'User controller hazırlanıyor...'
  });
}; 